
class Deck extends HTMLElement {
    constructor() {
        this.deck = [];
    }

    addCard(card) {
        //Adds a card to the deck

    }

    removeCard(card) {
        //Removes a card from the deck

    }

    drawCard() {
        //Pops and returns a card form the deck

    }

    renderDeckHelper() {
        //Helper function to help render cards
    }
}